% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/illustration_functions.R
\name{plot_event_tree}
\alias{plot_event_tree}
\title{Plot Event Tree}
\usage{
plot_event_tree(edge_event_table, tree)
}
\arguments{
\item{edge_event_table}{A data frame containing edge event information. This is typically generated by the
\code{create_edge_event_table} function:
\itemize{
\item edge_name: Edge identifier ("parent_child" format)
\item edge_label: Event labels for the edge
\item n_events: Number of events on the edge
}}

\item{tree}{An igraph tree object representing the phylogenetic structure}
}
\value{
A list containing two elements:
\itemize{
\item graph_ob: The igraph object with added event information
\item tree_plot: A ggplot2 object showing the rendered tree
}
}
\description{
This function generates a visual representation of an event tree using the provided edge event table
and tree structure. The tree is plotted with edges labeled by event information and nodes colored
by their type (normal or tumor). The function returns both the graph object and the plot for further
customization or analysis.
}
\details{
The function performs these steps:
\enumerate{
\item Converts tree to edge list format
\item Creates node table with clone types (normal/tumor)
\item Merges edge event information with tree structure
\item Creates a dendrogram layout visualization with:
\itemize{
\item Edges labeled with copy number events
\item Nodes colored by clone type
\item Node labels showing clone names
}
}
}
\note{
The visualization uses:
\itemize{
\item Node size of 20
\item Text size of 5
\item Grey edges
\item 2.5mm label dodge for edge labels
\item void theme for clean background
}
}
\examples{
\dontrun{
library(igraph)
library(ggraph)

# Create sample tree and edge events
tree <- make_tree(3, 2)
edge_events <- create_edge_event_table(your_events, tree)

# Create plot
result <- plot_event_tree(edge_events, tree)
print(result$tree_plot)
}

}
